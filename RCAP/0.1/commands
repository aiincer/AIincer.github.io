// - over:
  //start
    verbindung zum server
    startet session
  //stop
    verbindung zu server trennen
  //login ^account^ ^passwort^
    ^coount^: benutzername
    ^passwort^: passwort
    logt account in session ein
: - client-side:
  :debug ^bool^
    ^bool^: an/aus
    schaltet den debug-mode an oder aus
    (standard mäßig aus)
# - library:
  #install ^library^
    library installieren

########

// - over:
  //start                           🟩 0.1
  //stop                            🟩 0.1
  //login ^account^ ^passwort^      🟩 0.1
: - client-side:
  :debug                            🟩 0.1
# - library:
  #install                          x
