// - over:
  //start
    verbindung zum server
    startet session
  //stop
    verbindung zu server trennen
  //login ^account^ ^passwort^
    ^coount^: benutzername
    ^passwort^: passwort
    logt account in session ein
: - client-side:
  :debug ^bool^
    ^bool^: an/aus
    schaltet den debug-mode an oder aus
    (standard mäßig aus)
~ - manager:
  ~start
    startet die gewählte client side library
  ~install ^library^
    installiert client side library
  ~run ^library^
    führt client side library aus
# - library:
  #install ^library^
    library installieren

########

// - over:
  //start                           🟩 0.1
  //stop                            🟩 0.1
  //login                           🟩 0.1
: - client-side:
  :debug                            🟩 0.1
  :#install                         
  :#run                             
~ - manager:
  ~start                            
  ~install                           
  ~run                              
# - library:
  #install                          x
